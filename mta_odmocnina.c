#include <stdio.h>

int mta_odmocnina(unsigned int m, double x);

int main(void)
{
}

int mta_odmocnina(unsigned int m, double x)
{
     if (m == 0)
     {
          printf("Takto to bohužiaľ nepôjde.\n");
     }
}